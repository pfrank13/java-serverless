datasources:
  #This will be the config that is used during migrations
  default:
    dialect: MYSQL #This apparently does nothing
    jdbc-url: jdbc:tc:mysql://localhost/booksdb?TC_DAEMON=true
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
    maximum-pool-size: 1
    minimum-idle: 1
flyway:
  datasources:
    default:
      locations: classpath:migrations